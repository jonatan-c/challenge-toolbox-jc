{
  "name": "challenge-toolbox-backend",
  "version": "1.0.0",
  "description": "REST API that fetches and reformats CSV data from an external API",
  "main": "src/index.js",
  "scripts": {
    "start": "node src/index.js",
    "test": "mocha --timeout 10000 test/**/*.test.js"
  },
  "dependencies": {
    "axios": "^1.6.0",
    "express": "^4.18.2"
  },
  "devDependencies": {
    "chai": "^4.3.10",
    "mocha": "^10.2.0",
    "nock": "^13.4.0",
    "sinon": "^17.0.0"
  },
  "engines": {
    "node": ">=14"
  }
}
